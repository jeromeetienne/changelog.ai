// node imports
import Path from 'node:path';
import Url from 'node:url';

///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////
//	Project Root
///////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////

/**
 * Absolute path to the package root, resolved two levels up from `src/misc/`
 * (dev, via tsx) or `dist/misc/` (built). Used to locate bundled assets such as
 * `dotclaude_folder/`.
 */
export const PROJECT_ROOT = Path.resolve(Path.dirname(Url.fileURLToPath(import.meta.url)), '..', '..');
